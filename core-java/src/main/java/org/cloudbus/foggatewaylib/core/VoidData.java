package org.cloudbus.foggatewaylib.core;

/**
 * Same as {@link Data} but to be used when no data is required in input.
 * It does nothing, it is just clearer seeing "Void" when no data is actually passed.
 *
 * @author Riccardo Mancini
 */
public class VoidData extends Data {}
